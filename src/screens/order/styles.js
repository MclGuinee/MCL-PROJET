import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff"
  },

  cardStyle: {
    marginBottom: 30
  }
});

export { styles };